"""Минимальная пре-LLM блокировка явно небезопасных запросов"""

import re

_UNSAFE = re.compile(
    r"(убить|убийство|террор|бомб|самоубийств|взлом|наркотик)",
    re.IGNORECASE,
)

UNSAFE_REFUSAL = (
    "Я не могу помочь с этим запросом. "
    "Если Вам нужна срочная помощь, обратитесь, пожалуйста, в службы экстренной поддержки или к специалистам очно."
)


def is_unsafe_question(text: str) -> bool:
    return bool(_UNSAFE.search((text or "").strip()))
